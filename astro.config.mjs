import { defineConfig } from "astro/config";

// Static marketing site. Set `site` to the production URL before launch so
// absolute URLs (sitemap, OG tags) resolve correctly.
export default defineConfig({
  site: "https://sello.build",
});
